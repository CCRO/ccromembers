<!DOCTYPE html>
<html>
<head>
  <title>CCRO - <%= params[:controller].capitalize %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <% if current_user %>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">

        <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>

        <!-- Be sure to leave the brand out there if you want it shown -->
        <a class="brand" href="#">CCRO</a>

        <!-- Everything you want hidden at 940px or less, place within here -->
        <div class="nav-collapse">
            <ul class="nav">
              <!-- Research Center Dropdown -->
              <li <%= 'class=active' if params[:controller] == 'articles' || params[:controller] == 'groups' || params[:controller] == 'discussions' || params[:controller] == 'documents' || (params[:controller] == 'reports' && params[:action] == 'dashboard') %>><a href="/documents"><i class="icon-book icon-white"></i> Resource Center</a></li>
              <li><a href="#"><i class="icon-calendar icon-white"></i> Events</a></li>
              <li <%= 'class=active' if params[:controller] == 'companies' || params[:controller] == 'people' %>><a href="/people"><i class="icon-user icon-white"></i> Directory</a></li>
            </ul>
         </div>
        <div class="navbar-text pull-right">
          Logged in as <a data-toggle="modal" href="#modal-current_user"><%= current_user.email %></a>
        </div>

      </div>

    </div>
  </div>
  <%= render 'shared/subnav' %>
<% end %>
	<div class="container">
		<% flash.each do |key, msg| %>
    	  <div id="notice_<%= key %>" class="alert alert-<%= key %>" style="position: fixed; z-index: 1000; top: 210px; margin-left: 300px;">
          <a class="close" data-dismiss="alert">×</a>
          <strong><%=  key.to_s.capitalize %>!</strong> <%= msg %>
        </div>
		    <%= content_tag :script, :type => "text/javascript" do -%>
		        setTimeout("new Effect.Fade('notice_<%= key %>');", 15000);
		    <% end %>
		<% end %>
<!--Body content (BEGIN)-->
<%= yield %>
<!--Body content (END)-->
<% if current_user %>
	<div class="clearfix"></div>
    <footer>
        <div class="row">
            <div class="span6">
                <div style="text-align: center; padding: 10px;" class="well">
                    Committee of Chief Risk Officers (CCRO) is an independent non-profit corporation of member companies.&nbsp;
                </div>
            </div>
            <div class="span6" style="text-align: right;">
                <a href="/application/jobs"><b>We're Hiring!</b></a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="#">About</a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="#" onclick="#">Contact</a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="#">Terms</a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="#">Privacy</a>
            </div>
        </div>
    </footer>  
    </div>

  <div class="modal hide fade" id="modal-window">
    <div class="modal-header">
      <a href="#" class="close">×</a>
      <h3>Loading...</h3>
    </div>
    <div class="modal-body center">
        <%= image_tag "loading.gif" %>
    </div>
    <div class="modal-footer">&nbsp;</div>
  </div>
  <div class="modal hide fade" id="modal-current_user">
    <div class="modal-header">
      <a href="#" class="close">×</a>
      <h3><%= current_user.name %></h3>
    </div>
    <div class="modal-body center">
	<table cellpadding="10">
		<tr>
			<td>
      <ul class="thumbnails">
        <li class="span2">
          <a href="#" class="thumbnail">
            <%= gravatar_for current_user %>
          </a>
        </li>
      </ul>
	  </td>
	  <td valign="top">
      <p>Company: <%= current_user.company.name if current_user.company %><br />
      Role: <%= current_user.role.capitalize if current_user.role %><br />
      Email: <%= mail_to current_user.email %><br />
      Access Token: <%= raw '&bull;' * current_user.access_token.length %> <%= clippy(current_user.access_token) %></p>
	  </td>
	  </tr>
	  </table>
    </div>
    <div class="modal-footer">
      <%= button_to 'Logout', logout_path, :class => 'btn-danger' %>
      <%= button_to 'View Company', current_user.company if current_user.company %>
      <%= button_to 'View Profile', current_user %>
    </div>
  </div>
  <% end %>
</body>
</html>
