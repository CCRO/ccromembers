<%= form_for(@polling_session) do |f| %>
  <% if @polling_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@polling_session.errors.count, "error") %> prohibited this polling_session from being saved:</h2>

      <ul>
      <% @polling_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
