<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :role %><br />
    <%= f.select :role, options_for_select(ROLES, @company.role) %>
  </div>
  <div class="field">
    <%= f.label :highrise_id %><br />
    <%= f.text_field :highrise_id %>
  </div>
  <div class="field">
    <%= f.label :freshbooks_id %><br />
    <%= f.text_field :freshbooks_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
